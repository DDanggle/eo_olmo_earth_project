# Streaming review snapshot — 2026-09-08 afternoon KST

- Local source revision reviewed: `ac36ed5`.
- Remote JSON/log/metadata copy: approximately 15:30 KST. Read-only transfer through `./bin/nx sh`.
- Copied 36 baseline reports and 22 architecture reports; no raw images, test masks, probability arrays,
  or model checkpoints were downloaded. Current source copies are retrospective, not pre-run snapshots.
- The transfer also requested two remote `config/` files, but they were absent there. Tar reported those
  missing paths; other requested files were received. Registration evidence is the **local committed**
  `config/kurosiwo_streaming_prereg_v0.json` and `config/streaming_updater_arch_prereg_v0.json`, not this
  incomplete remote config capture. Do not treat the archive as proof of runtime protocol enforcement.
- `summary.json` is generated by `code/audit_streaming_progress.py`. No scoring on unseen labels.
- `kurosiwo_s1_cache/meta.jsonl` has 7,000 unique IDs; pairwise split ID and activation disjointness
  is checked. Polygon overlap across different activations is not audited here.
- `logs/ks_update_gru_s1.log` has 30 nonfinite validation epochs and a false success string.
  This is execution invalidity, not evidence against S1 streaming utility.
- Full-training-teacher CPU std diagnostic: 4,000 finite tiles / 7,077,888,000 values;
  trainer torch.std = 0.48853766918182373, chunked float64 = 0.4885376707939189;
  torch 2.13.0+cu130, CUDA hidden, 4 CPU threads, ~28.3 GB tensor, elapsed 66.660 s.
  Only the CPU reduction was reproduced, not the failed training trajectory.
- Active training files were not edited or reuploaded. The separate read-only diagnostic is
  `/home/work/data/olmoearth/review_20260908_1530/probe_kurosiwo_numeric.py`.
- Follow-up full train/validation input scan found one bad validation ID, `ks_04357`.
  `numeric_input_scan.json` records the 4,000/1,000 finite checks; `invalid_validation_tile.json`
  records 1,338 nonfinite raw values and 327 contaminated feature tokens intersecting valid labels.
  This invalidates treating finite printed validation AP as sufficient proof of clean evaluation.
  It is not a test-label audit and does not establish whether the raw defect predates export.
