# Optional geospatial dependencies for the K-Earth public-data ingestion pipeline.
# The core validation and CSV adapters use only the Python standard library.
numpy==2.5.2
pyproj==3.7.2
pyshp==2.3.1
shapely==2.1.2
