{
  "schema": "task2-extension-preregistration-v0",
  "status": "REGISTERED_NO_DOWNLOAD_NO_RUN",
  "date": "2026-09-02",
  "doc": "docs/MULTITASK_EXTENSION_2026_09_02.md",
  "question": "Does the frozen-OlmoEarth cache-reuse advantage measured on Sen12Landslides (M65: P4 .2722 vs best raw .1966, 7/8 regions vs best raw arm) reproduce on a second and third task with a different domain, under the identical recipe and geographic leave-one-region-out protocol?",
  "selection_rules_fixed_before_data": [
    "10 m Sentinel-2 multi-temporal input (Sentinel-1 optional)",
    "pixel masks (segmentation) preferred; tile labels only as head sensitivity",
    "natural geographic grouping enabling leave-one-region-out",
    "contract already solved (rslearn-format or OlmoEarth-evaluated public dataset)",
    "domain priority: disaster > land change > static land cover"
  ],
  "tasks": {
    "task2_primary": {
      "name": "satlas_solar_farm",
      "source": "gs://ai2-olmoearth-projects-public-data/evals/partner_tasks/solar_farm.tar (rslearn_projects olmoearth_evals README); config in olmoearth_run_data/satlas_solar_farm",
      "input": "Sentinel-2, 4 timesteps, 12 bands (model view B02 B03 B04 B08 B05 B06 B07 B8A B11 B12 B01 B09)",
      "label": "binary segmentation mask",
      "geographic_grouping": "UTM zone (fallback: continent), balanced into 8 folds",
      "why": "global coverage gives real geographic shift; binary segmentation matches the landslide recipe exactly; anthropogenic, season-independent target widens the domain axis"
    },
    "task3": {
      "name": "Sen1Floods11",
      "source": "https://github.com/cloudtostreet/Sen1Floods11 (also in OlmoEarth and PANGAEA evaluation suites)",
      "input": "Sentinel-1 + Sentinel-2, single date (requires single-timestep handling in the cache extractor)",
      "label": "flood segmentation mask",
      "geographic_grouping": "11 flood events = event-level leave-one-out",
      "why": "disaster domain with mandatory radar; tests whether cache reuse survives a water-body task"
    },
    "head_sensitivity": {
      "name": "forest_loss_driver",
      "source": "olmoearth_run_data/forest_loss_driver (do not modify those two files; copy); partner tar",
      "input": "Sentinel-2 4 pre + 4 post",
      "label": "10-class tile classification",
      "why": "before/after structure isomorphic to landslides; classification head only"
    },
    "deferred": [
      "mangrove (100k samples)",
      "PASTIS-R (variable timestep rule needed)",
      "SenForFlood (254 GB)",
      "lfmc (regression)"
    ],
    "rejected_contract_mismatch": [
      "xBD (VHR)",
      "Landslide4Sense (single date; rejected earlier)",
      "HLS Burn Scars (Landsat)",
      "DynamicEarthNet (Planet)"
    ]
  },
  "protocol_identical_to_sen12": {
    "arms": [
      "P2 UNet3D raw",
      "P3 U-TAE raw",
      "P4 frozen OlmoEarth 768x32x32 + EmbDecoder"
    ],
    "recipe": "40 epoch, BCEWithLogits, pos_weight=neg/pos capped 50, Adam lr 1e-3 wd 1e-4, batch 16, best epoch on the fold's val region",
    "seeds": [
      1,
      2,
      3
    ],
    "metrics": [
      "positive-tile macro IoU (primary)",
      "FP-matched operating point IoU",
      "tie-correct pixel AP"
    ],
    "statistical_unit": "fold (region); seeds are repeated measurements"
  },
  "verdict_rule": {
    "reproduces": "P4 beats the best raw arm in >= 5 of 8 folds",
    "domain_dependent": "otherwise; the paper narrows the reuse claim to sparse natural-change tasks and reports this as a boundary, not a failure",
    "no_post_hoc_relaxation": true
  },
  "few_shot_followup": {
    "arms": [
      "A0 reuse",
      "A1 decoder-only adaptation",
      "A4 raw few-shot"
    ],
    "excluded": "A2 CacheTune family (MS-94 stop rule applies across tasks)",
    "K": [
      5,
      20
    ],
    "development_folds": 2,
    "manifest_rule": "same as cachetune_pt0 (event/component grouping, 10 km blocks, 3 km buffer, >= 40% positive support)"
  },
  "cost_estimate": {
    "cache_extraction": "one pass over all tiles x 4 timesteps on GPU1",
    "training_runs": "8 folds x 3 arms x 3 seeds = 72 runs; ~16 min each on Sen12 => ~19 GPU hours, x1.5 under sharing"
  },
  "new_files": [
    "code/fetch_task2_solar_farm.sh",
    "code/audit_task2_contract.py",
    "code/build_task2_geo_folds.py",
    "code/extract_task2_cache.py",
    "code/run_task2_confirmatory.sh"
  ],
  "protected_files_untouched": [
    "pilot_sen12_gp_heads.py",
    "sen12_official_baselines.py",
    "extract_sen12_fold_cache.py",
    "audit_sen12_fold_cache.py"
  ],
  "step0_parallel_cpu": "Korea preflight without opening labels: AIHUB_CUBE_V2_CONTRACT 40-tile pilot gate, model_view_10band, keep M10 spatial split",
  "forbidden_claims": [
    "OlmoEarth is superior to all GeoFMs (Prithvi/Clay/TerraMind not run)",
    "reuse always beats retraining",
    "Korea used for development"
  ],
  "references": [
    "https://arxiv.org/abs/2511.13655",
    "https://github.com/allenai/rslearn_projects/blob/master/rslp/olmoearth_evals/README.md",
    "https://arxiv.org/abs/2412.04204",
    "https://openaccess.thecvf.com/content/CVPR2024/html/Scheibenreif_Parameter_Efficient_Self-Supervised_Geospatial_Domain_Adaptation_CVPR_2024_paper.html",
    "https://openaccess.thecvf.com/content/ICCV2025/html/Thoreau_Parameter-Efficient_Adaptation_of_Geospatial_Foundation_Models_through_Embedding_Deflection_ICCV_2025_paper.html",
    "https://arxiv.org/abs/2507.22291",
    "https://github.com/GEOHUM-PLUS/SenForFlood",
    "https://github.com/cloudtostreet/Sen1Floods11"
  ],
  "addendum_v1_2026_09_03_fewshot_confirmatory_registered_before_label_look": {
    "why": "The registered Task-2 plan replicated M65 (full-data P2/P3/P4) but did not replicate the new headline (MS-96/97 few-shot decision). Reviewers would note the label-budget decision rests on one task.",
    "status_at_registration": "solar_farm.tar downloaded and SHA-sealed (e08e5349a51ba057f4a27cd6878349cb31f97c17a253f437dabe627d905bd39f); archive not yet inspected; no label statistics viewed",
    "design": {
      "arms": [
        "A0",
        "A1",
        "A4w",
        "A4h"
      ],
      "K": [
        5,
        20
      ],
      "folds": "all 8 geographic folds; if compute forces a cut, the first 5 folds in the sealed fold order are used and this is decided before any Task-2 metric is read",
      "support": [
        "stratified (>= ceil(0.4K) positive tiles)",
        "random (uniform from pool, seeds 101-103)"
      ],
      "metrics": [
        "FP-matched positive-tile macro IoU (primary)",
        "tie-correct AP",
        "IoU@0.5"
      ],
      "schedule": "fixed-update 300; fixed-exposure as sensitivity",
      "statistical_unit": "fold"
    },
    "success_rule": {
      "replicates": "A1 - A4w >= +0.01 in >= 6/8 folds at K=5 (>= 4/5 if 5 folds) AND same direction at K=20",
      "representation_rule": "A1 - A4h >= +0.01 in >= 6/8 (4/5)",
      "fails": "otherwise the label-budget decision is reported as landslide-specific; no relaxation"
    },
    "prerequisite_full_data_arms": "source P4/P2 decoders per fold must first be trained under the Sen12 recipe (this is the M65-style step) — reported, but the paper headline for Task-2 is the few-shot replication",
    "independence_note": "Solar Farm is an official OlmoEarth partner task: strong for the Ai2 link, not a fully independent external test. A public external task (Sen1Floods11 or an EarthShift split) is registered as Task-3 for independence."
  },
  "A3_encoder_peft_registered_2026_09_03": {
    "arm": "A3 = OlmoEarth v1 Base encoder PEFT (LoRA on attention q/v or rslearn APLA; rank 8 primary) fine-tuned on the same K support tiles from raw pixels, then re-embedded; decoder from A1 protocol",
    "purpose": "closes 'reuse vs re-embed': today's 'retrain' arm is raw UNet, so the question 'why not adapt the OlmoEarth encoder itself?' is open",
    "interpretation_fixed": {
      "A1_wins": "strong cache-compatible post-training result",
      "A3_slightly_wins": "re-embedding only when cost is justified; report accuracy-vs-recompute frontier (raw bytes, GPU s, cache invalidation)",
      "A3_clearly_wins": "shift the paper to finding where cache validity breaks"
    },
    "scope": "development folds first (china, chimanimani), K=20, 3 seeds; confirmatory only if gate rules are met; costs must be reported per arm"
  },
  "addendum_v2_2026_09_03_sample_unit_and_folds_fixed_before_metrics": {
    "contract_facts_from_partial_audit": "variable windows ~410-490 px @10 m, S2 4 timesteps x 12 bands (+B10), 58 UTM zones, binary label PNG, official split train/val only, 201 same-CRS bounds overlaps",
    "sample_unit": "one 128x128 px chip per window = the window's CENTER chip (label-blind, deterministic). Keeps Sen12 tile geometry so P4 EmbDecoder (768x32x32), UNet3D and all few-shot arms run unchanged. Sensitivity (if compute allows): 4 chips per window on a fixed 2x2 grid around the center.",
    "exclusions": "windows with <4 completed S2 timesteps or incomplete band groups are excluded before any metric (audit lists them)",
    "raw_cache_contract": "raw_u16 = 10 bands (B02 B03 B04 B08 B05 B06 B07 B8A B11 B12) x 4 timesteps x 128 x 128 uint16; months from layer item timestamps; emb_fp16 = frozen OlmoEarth v1 Base 768x32x32 on the same 12-band (B01/B09 present, B10 dropped) 4-timestep stack; mask_u8 = label_raster center crop >0",
    "geographic_folds": "group = UTM zone; windows whose bounds overlap in the same CRS are merged into one group (leakage guard). Groups are packed into 8 folds by greedy balancing of window counts in sorted-zone order; val region for a fold = next fold in the sealed order (as in Sen12). Fold assignment sealed with SHA before any model is trained.",
    "full_data_prerequisite": "P4 and P2 source decoders per fold with the Sen12 recipe (40 epoch, BCE, pos_weight cap 50); reported as the M65-style replication",
    "headline": "few-shot replication per addendum_v1 (A0/A1/A4w/A4h, K=5/20, stratified+random)",
    "compute_note": "~10k chips ≈ 1.5x Sen12; embedding extraction one pass on GPU1"
  }
}