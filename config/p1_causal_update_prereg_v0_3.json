{
 "schema": "p1-causal-update-prereg-v0.3",
 "registered_at": "2026-09-20 (v0.3, registered before its run; replaces the retired frozen-readout contract with matched trained heads for every arm)",
 "status": "registered before execution; P1 first independent experiment (design doc §8.3, audit §6)",
 "question": "With the SAME small head recipe (temporal-contrast labels, same epochs, same train tiles) fitted on top of each arm's state, does state memory (gru_dt, prefix, resid_q) confirm earlier or localise better than the latest single observation, at matched tile-level pre-event FAR?",
 "data": "arrival_v0/hiroshima (single_fp16, prefix_fp16, meta); readout resolution_contract_v2/p4_native_control/holdout_hiroshima_seed1 (frozen); normalisation stats from the sealed train cache as in training",
 "split": "tile-id hash into 5 blocks: train {0,1,2}, val {3}, test {4}. Pseudo-spatial (adjacent tiles may share blocks) — stated limitation. Single event date for the whole region: tiles are spatial replicates of ONE event; CIs are over tiles and do not license event-level generalisation.",
 "state_init": "m_0 = single_0 for all arms (no lookahead)",
 "arms": {
  "last": "m_i = single_i",
  "mean": "running mean of singles",
  "ema": "m_i = (1-a) m_{i-1} + a single_i, a learned scalar",
  "gru": "ConvGRU 1x1 (MS-116 architecture) on [m, single_i]",
  "gru_dt": "ConvGRU with Δt embedding (MS-116-arch gru_dt)",
  "resid_q": "PROPOSED: m_i = m_{i-1} + g ⊙ f([m, u, u - P(m)]), gate g from [m, u, u-P(m), q_i, Δt_i] (quality- and innovation-conditioned correction); no uncertainty head in v0",
  "prefix": "full re-encoding of the arrival prefix (teacher; cost reference, not a physical ceiling)",
  "latest_head": "latest-only control v2: 1x1 head on the single-observation state trained with TEMPORAL-CONTRAST labels — for positive train tiles, pre-event steps carry the zero label with the same weight as post-event steps carry the mask; no pos_weight; negative tiles included. Automatic invalidation if realised test pre-event FAR >= 3x budget.",
  "meta_only": "control: state evolved from m_0 with Δt/day-of-year/region only, no observations after step 0 (detects memorised event timing)",
  "sealed12": "readout sanity only: ONE decision per tile (area of the full-year sealed state >= threshold); reported as fraction of positive test tiles above threshold and pre-event-free false alarm on negative tiles; no latency"
 },
 "training": "learned arms: teacher = prefix state at each step, MSE, closed-loop rollout from step 1..14, AdamW 1e-3, 30 epochs, batch 8 tiles, seed 1 (seeds 2-3 if seed 1 shows an effect); GPU0",
 "detection_rule": "predicted-positive area (readout prob > .5) in window 8..120 >= A_thr; A_thr set PER ARM on the validation block so that the pre-event step-level false-alarm rate = 5% (steps with no post-event observation in the prefix). Test reports the realised pre-event FAR.",
 "metrics": [
  "days-to-confirmation per positive test tile: date of the first post-event step with detection minus event_date (fallback: first post-event acquisition date); censored if never",
  "confirmation rate within the window",
  "mean IoU on post-event steps",
  "realised pre-event FAR on test",
  "paired bootstrap (tiles) of days-to-confirmation and of IoU: resid_q vs gru_dt and vs last",
  "cost: encoded timestep-units per tile per arrival (1 for streaming arms, min(c,12) for prefix)",
  "latency reference B: days from the first VALID post-event observation (clear >= .5) to confirmation",
  "confirmation rate and number of never-confirmed (censored) tiles reported alongside every latency median",
  "meta_only must not confirm above the pre-event FAR; if it does, timing memorisation is flagged"
 ],
 "decision_rules": {
  "memory_helps": "gru_dt+head or prefix+head beats last+head on post-event IoU by >= .03 (tile-paired bootstrap CI excluding 0) at 10% tile-level FAR with confirmation rate not lower by more than 5 points",
  "memory_no_help": "otherwise: state memory does not help this task beyond the latest observation; T method track closed",
  "latency": "reported (latency A/B, censored) but not a pass criterion since last+head already sits at 0 days"
 },
 "not_claimed": [
  "generalisation beyond the single hiroshima event",
  "label-budget parity with the sealed readout (all heads share one recipe instead)"
 ],
 "objectives_note": "v0.1 keeps teacher-MSE training for comparability; the evidence-based early-confirmation objective (design doc §9.2) is v1 and needs its own contract",
 "far_budget_unit": "TILE level: a validation tile is a false alarm if ANY pre-event step's predicted-positive area >= threshold; threshold chosen so that 5% (and 10%) of validation tiles are false alarms. Test reports realised tile-level pre-event FAR.",
 "arms_v0_3": {
  "last+head": "head on single_i (== latest_head v2)",
  "gru_dt+head": "head on Δt-GRU rollout state (teacher-MSE updater from v0.2 recipe, then head)",
  "prefix+head": "head on the causal full re-encoding prefix state",
  "resid_q+head": "head on the v0 corrector state",
  "reference": "frozen-readout arms kept for continuity but NOT used for decisions"
 }
}