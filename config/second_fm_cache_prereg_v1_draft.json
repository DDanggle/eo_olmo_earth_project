{
  "schema": "second-fm-cache-preregistration-v1-draft",
  "status": "REGISTERED_2026_09_04_commit_before_any_B_v1_output; the in-flight Clay v0 run (16->32 bilinear, v0 protocol) is EXPLORATORY 'interpolated deployment adapter' evidence only",
  "date": "2026-09-04",
  "parent": "config/second_fm_cache_prereg_v0.json at commit f2f816a7f4e234c1fbcf31c99114eff7c6034ce2",
  "why_revision_is_needed": [
    "Clay v1.5 patch size 8 yields a native 16x16 grid for the project's 128 px tiles, so upsampling every model to 32x32 is not an information-matched comparison",
    "AlphaEarth is an annual 10 m 64-d embedding product and cannot share an event-timing gate with Sen12 without temporal leakage",
    "one additional model family is evidence across two families, not a universal product-agnostic principle"
  ],
  "tracks": {
    "B1_common_physical_cache": {
      "purpose": "readout-controlled compact-cache comparison at common physical support and equal decoder parameters; not a pure causal estimate of representation family because PCA is fit separately per model",
      "grid": "16x16 at 80 m support",
      "olmoearth": "fixed 2x2 arithmetic mean from 32x32 to 16x16",
      "clay": "native 16x16 patch grid from a 128 px Sentinel-2 tile",
      "channels": "source-only IncrementalPCA to 256 dimensions, fit separately per model with the same algorithm and sample IDs",
      "decoder": "exactly identical 256-channel decoder, seeds, folds, optimizer and metrics"
    },
    "B2_native_product": {
      "purpose": "usable product performance, not an architecture-causal comparison",
      "contract": "retain each product's native grid, channels, normalization and temporal receptive field; report trainable parameters, FLOPs, cache bytes and cold extraction cost"
    }
  },
  "candidates": [
    {
      "name": "Clay v1.5",
      "role": "primary like-for-like spatial ViT family",
      "documented_contract": "Sentinel-2 L2A 10 bands, patch size 8, encoder dimension 1024, Apache-2.0",
      "temporal_rule": "encode each selected timestep independently, then arithmetic mean; no pooling choice after metrics are seen",
      "tasks": [
        "Sen12Landslides",
        "Task-2 Solar Farm"
      ],
      "tracks": [
        "B1_common_physical_cache",
        "B2_native_product"
      ]
    },
    {
      "name": "AlphaEarth Satellite Embedding V1 annual",
      "role": "shipped embedding-product comparison",
      "documented_contract": "annual 2017-2025 COGs, 10 m, 64-d unit vectors, CC-BY 4.0, provider-pays GCS as of July 2026",
      "tasks": [
        "Task-2 Solar Farm",
        "future static land-cover task"
      ],
      "excluded": "event-timed Sen12 headline; annual receptive field can include observations after the event"
    },
    {
      "name": "Prithvi-EO-2.0",
      "role": "contract-shift sensitivity after Clay",
      "documented_contract": "HLS six bands, 30 m, temporal ViT, Apache-2.0",
      "warning": "S2-to-HLS band mapping and GSD change are part of the product contract and prevent a clean model-only comparison"
    }
  ],
  "claim_ladder": {
    "clay_two_tasks_pass": "evidence across two spatial GeoFM families; not universal product agnosticism",
    "alphaearth_static_pass": "the workflow extends to a ready-made annual embedding product for static mapping only",
    "product_agnostic": "requires at least two non-OlmoEarth families plus an independent Task-3 under precommitted task-appropriate contracts"
  },
  "metrics": [
    "positive-tile macro IoU",
    "tie-correct AP",
    "deployment-threshold IoU with threshold selected on source validation",
    "cold extraction and warm-head cost",
    "cache bytes"
  ],
  "forbidden": [
    "bilinear upsampling of Clay 16x16 tokens presented as common-resolution evidence",
    "forcing AlphaEarth annual embeddings into an event-timed headline",
    "calling Clay source-code v1.0 and model-weight v1.5 a verified release pair without two weight artifacts",
    "claiming universal superiority or product agnosticism from Clay alone",
    "choosing B1 or B2 as the headline after seeing which is better",
    "treating the active v0 16x16-to-32x32 bilinear cache as either the B1 common-physical or B2 native confirmatory result",
    "comparing Clay FP-matched IoU against a historical raw report whose false-positive budget was set by a different A0 model"
  ],
  "addendum_v1a_2026_09_04_diagnostics_registered_before_metrics": {
    "why": "MS-101 (Clay v0, interpolated) failed cache-first. Before any B-v1 headline, three confounders are separated with single-seed diagnostics on the 8 confirmatory Sen12 folds, all trained by code/cache_decoder_train.py (re-implementation of the sealed pilot P4 recipe; calibrated on OlmoEarth 32x32 hiroshima seed 1 against the sealed pilot before any diagnostic is read).",
    "caches": {
      "olmo_cache_pool16": "OlmoEarth 32x32 -> 2x2 mean -> 768x16x16 (resolution confounder control)",
      "clay_cache_native16": "Clay v1.5 native 16x16, per-timestep encode, mean over 12 timesteps",
      "clay_cache_native16_last": "Clay v1.5 native 16x16, last timestep only (temporal-pooling sensitivity)",
      "galileo_cache": "Galileo base (nasaharvest, ICML 2025) S2-only, T=12 native, patch 4 -> 768x32x32, 4x64px tiling as OlmoEarth",
      "prithvi_cache": "Prithvi-EO-2.0-300M (temporal 3D-patch ViT), 6 S2 bands mapped to HLS order at 10 m (contract shift acknowledged), T=4 (indices 1,4,7,10 of 12), patch (1,16,16) -> 1024x8x8; decoder G=8 (8->16->32->128, identical params)",
      "clay_cache_in256": "Clay v1.5 with the 128 px tile bilinearly upsampled to 256 px before encoding (input-side adapter; gsd still declared 10 m), native 32x32 tokens, mean over 12 timesteps -> 1024x32x32 (resolution-matched to OlmoEarth)",
      "galileo_cache_groupcat": "Galileo base, mean over T but the 5 seen S2 band-group tokens concatenated (3840x32x32) instead of averaged; readout sensitivity — tests whether naive group averaging (not the representation) caused the Galileo deficit"
    },
    "readout": {
      "olmo_pool16_still_beats_raw_>=6/8": "token resolution is not the reason Clay v0 failed",
      "clay_native16_beats_raw_>=6/8": "the v0 failure was the interpolation adapter; Clay joins the family evidence",
      "clay_last<clay_mean": "temporal averaging helps; not the cause",
      "galileo_beats_raw_>=6/8": "a second temporally-native family reproduces cache-first -> the lead is 'temporal-native multimodal masked pretraining'; then run 3 seeds + few-shot A0/A1 on Galileo",
      "galileo_fails": "cache-first is OlmoEarth-specific among the families tested; report as such",
      "prithvi": "contract-shift sensitivity only (band/GSD mismatch is part of the product contract); counted as family evidence only if it beats raw >=6/8 despite the shift; failure is not evidence against the family",
      "clay_in256_vs_native16": "if in256 >> native16 then token resolution dominates the Clay deficit; if in256 still < raw in >=5/8, the Clay representation itself is not cache-worthy for this task"
    },
    "raw_reference": "raw_strong P2 per fold from artifacts/confirmatory_8region_summary.json",
    "status": "REGISTERED_AT_COMMIT_before_any_diagnostic_metric_is_read",
    "first_result_note": "olmo_cache_pool16 (6 folds, seed 1): macro .225 vs 32x32 .263 same folds, beats raw 3/6 -> token resolution alone removes most of the OlmoEarth margin; read before Clay native results, recorded here for honesty",
    "extraction_audit_2026_09_05": {
      "file": "artifacts/second_fm_cache_audit.json",
      "galileo": "input scale matches Galileo pretraining stats (DN; S2 means ~1400-2750), 5/7 groups seen (S1, NDVI masked), months 0-11, layernorm on exit; token spatial autocorrelation present (adjacent cos .885 vs random .372); no dead channels",
      "clay": "metadata mean/std in DN (blue 1105 ...), waves/gsd/time/latlon per tutorial; 58/1024 constant channels; adjacent cos .88 vs random .50",
      "prithvi": "tokens near-collapsed (random cos .77) — contract shift (30 m model on 10 m) as registered",
      "verdict": "no extraction bug found; MS-102 stands, pending the groupcat readout sensitivity"
    }
  }
}
