{
 "schema": "seasonal-baseline-prereg-v0.1",
 "registered_at": "2026-09-20",
 "status": "registered before execution",
 "motivation": "MS-147 addendum: hokkaido normal pairs keep z>=2 at .308 (other regions .025-.037) even when both acquisitions are token-SCL clean, so the residual over-dispersion is not cloud. Hypothesis: it is seasonal surface change (snow / crop cycle) that a gap x season table fitted on three warm regions (LOEO) cannot represent.",
 "question": "Does an in-region seasonal baseline (table fitted on the SAME region's held-out tiles) bring hokkaido's normal z>=2 rate down to the level of the other regions without destroying event detection?",
 "data": "arrival_v0/<region>/single_fp16 + meta (base). NORMAL pair = consecutive clear>=.5 acquisitions not crossing post_index (as change_calibration v0.1). EVENT pair = last clear pre / first clear post on positive tiles, token mask >=.25 (as token calibration v0.2).",
 "split": "SWAPPED halves versus v0: fit on hash-odd tiles (A'), evaluate on hash-even tiles (B'). B' was never used for evaluation in v0, so v0.1 is a fresh confirmation on disjoint eval tiles.",
 "arms": {
  "loeo": "gap x season(DJF/MAM/JJA/SON) table from the other three regions (reference = current pipeline)",
  "inregion_season": "gap x season table fitted on half A of the target region",
  "inregion_month": "gap x month table fitted on half A (finer season axis; cells with <2000 tokens fall back to season cell)"
 },
 "metrics": [
  "normal_z2: fraction of normal-pair tokens (subsample .25) with z>=2 on half B",
  "event_auc: pooled token AUC mask vs non-mask on event pairs of half B",
  "event_tpr_z2 / fpr_z2 on event pairs"
 ],
 "gate": {
  "pass": "for hokkaido, inregion_season has normal_z2 <= .10 AND event_auc >= loeo event_auc - .03 (no absolute floor; the floor duplicates a detection question that this experiment does not ask)",
  "control": "for the other three regions, inregion_season normal_z2 <= loeo normal_z2 + .02",
  "fail_reading": "if normal_z2 stays > .15 with in-region tables, hokkaido over-dispersion is tile-specific (not seasonal) and a per-tile baseline (time-series of the same tile) is the next candidate; if event_auc collapses, the seasonal table absorbs the event signal (event confounded with season) and the result is a benchmark caveat, not a fix"
 },
 "compute": "GPU0 or CPU, minutes; no training",
 "allowed_claims_after": "a seasonal baseline row in the reliability track table; no downstream reader claim",
 "not_allowed": "changing the gate after seeing results; using the eval half for fitting; citing v0's numbers as a pass",
 "supersedes": "v0 (MS-148): the absolute AUC floor .75 was set above the LOEO reference (.703), a registration error; v0 stays recorded as fail."
}