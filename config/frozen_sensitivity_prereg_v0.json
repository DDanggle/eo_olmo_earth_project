{
  "schema": "frozen-sensitivity-prereg-v0",
  "registered_at": "2026-09-17",
  "status": "registered before execution; diagnostic, not confirmatory",
  "question": "Does frozen OlmoEarth v1 base (patch 4, 40 m tokens) change its readout decisions under (a) sub-token spatial grid offsets of 20 m and (b) month-label perturbations of identical pixels?",
  "model": "OlmoEarth v1 base via rslearn wrapper, token_pooling, patch 4, real acquisition dates from sealed contract (same as sealed P4 cache)",
  "folds": ["holdout_hiroshima", "holdout_indonesia"],
  "split": "test region tiles only",
  "readout": "resolution_contract_v2/p4_native_control/<fold>_seed1_best.pt, frozen; normalisation stats recomputed from the unperturbed train cache exactly as in training",
  "arms": {
    "ctrl": {"offset_px": [0, 0], "time": "real", "role": "control; also reproducibility check against sealed cache (expect token cos >= .995)"},
    "s20_y": {"offset_px": [2, 0], "time": "real", "role": "20 m half-token shift"},
    "s20_x": {"offset_px": [0, 2], "time": "real", "role": "20 m half-token shift"},
    "s20_xy": {"offset_px": [2, 2], "time": "real", "role": "20 m diagonal"},
    "s40_xy": {"offset_px": [4, 4], "time": "real", "role": "full-token shift; positive control for the alignment machinery (tokens should re-align by one cell)"},
    "t_synth": {"offset_px": [0, 0], "time": "synth_month", "role": "same month index, synthetic day/year (what extract_olmo_variants used)"},
    "t_plus1": {"offset_px": [0, 0], "time": "plus1", "role": "every timestamp moved +1 calendar month; pixels unchanged"},
    "t_shuffle": {"offset_px": [0, 0], "time": "shuffle", "role": "month labels permuted per tile (seeded); pixels and order unchanged"}
  },
  "spatial_handling": "tile viewed at (dy,dx) with 4-px edge replication on the far side; prediction map shifted back to the original frame; all metrics on the common window rows/cols 8..120 (2-token border excluded)",
  "metrics": {
    "primary_label_free": "decision_flip_rate = fraction of window pixels whose 0.5-threshold decision differs from ctrl",
    "secondary_label_free": ["prob_mae vs ctrl", "token_cos vs ctrl on aligned arms (time arms and s40_xy shifted by one cell)"],
    "labelled": "positive_patch_macro_iou in the window vs ctrl"
  },
  "decision_rules": {
    "yardstick": "s40_xy decision_flip_rate (full-token shift) is the harmless-perturbation floor",
    "spatial_axis_present": "mean decision_flip_rate over s20_* arms >= 2 x s40_xy flip rate AND >= 0.02 absolute, in both folds",
    "time_axis_present": "decision_flip_rate of t_plus1 or t_shuffle >= 2 x s40_xy flip rate AND >= 0.02 absolute, in both folds",
    "time_axis_inert": "all time arms flip rate < 0.005 and token_cos > .995 -> month encoding is effectively ignored at this contract; continuous-time post-training track closed for Sen12",
    "control_failure": "ctrl vs sealed token_cos < .99 -> extraction not reproducible; stop and audit before interpreting anything"
  },
  "not_claimed": ["cause of instability (aliasing vs boundary vs normalisation) is not separated here", "no generalisation beyond Sen12 S2 10-band contract", "no post-training result"],
  "compute": "GPU0 only; ~1435 tiles x 8 arms at ~6.5 tiles/s ~ 30 min; no sealed file modified"
}
