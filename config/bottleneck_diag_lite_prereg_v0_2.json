{"schema":"bottleneck-diag-lite-prereg-v0.2","registered_at":"2026-09-22","status":"registered before execution",
"supersedes":"v0.1 (MS-152): privileged Q1 .000 with Qwen3-VL while full_prefix recall .32 on the same items -> the 512px downsample (8 m/px) made 3-building changes invisible; Q3 at chance for all conditions. v0.2 fixes the DIAGNOSTIC DESIGN, not the readings.",
"changes":["frames for Q1/Q2 are the target quadrant cropped at native resolution (~512x512 px, 4 m/px), not the full AOI downsampled","MIN_NEW raised 3 -> 8 new buildings so that the silver change is plausibly visible; Q1 'no' quadrants require 0 new buildings in the window (unchanged)","Q3 (which quadrant has the newest construction) dropped: at chance for every condition in v0.1, not diagnostic","reader Qwen3-VL-8B zero-shot as v0.1; conditions, K=4, cutoffs, AOIs unchanged"],
"readings":"as v0/v0.1 (reader_bottleneck if privileged Q1 acc < .65; no_headroom if privileged - max(latest_k, change_topk) < .05 on Q1 and Q2; headroom_present if privileged beats latest_k and change_topk by >= .10 on Q2 with CI excl 0 AND full_prefix <= privileged; full_prefix_wins otherwise).",
"decision_use":"headroom_present -> start Chuseok prefix-visible labelling on these AOIs/questions; reader_bottleneck again -> SN7 quadrant questions are not readable zero-shot at 4 m; postpone labelling and consider a small SFT reader before any memory work",
"not_allowed":"changing K, cutoffs, AOI selection or readings after seeing results; citing silver-oracle headroom as human-gold headroom"}
